{
  "name": "pm-delivery-steward",
  "version": "1.0.2",
  "description": "A local, evidence-grounded delivery workspace for project managers and scrum masters.",
  "license": "MIT",
  "private": false,
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/runtime-security.js && node --check lib/csv-import.js && node --check lib/delivery-integrity.js && node --check lib/evidence-integrity.js && node --check lib/grounded-ai.js && node --check lib/input-validation.js && node --check lib/storage-integrity.js && node --check public/security.js && node --check public/app.js",
    "test": "npm run check && node --test",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "express": "^4.18.4",
    "express-rate-limit": "^8.2.2"
  }
}
