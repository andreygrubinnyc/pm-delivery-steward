{
  "name": "pm-delivery-steward",
  "version": "1.0.0",
  "description": "A local, evidence-grounded delivery workspace for project managers and scrum masters.",
  "license": "MIT",
  "private": false,
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/security.js && node --check public/app.js",
    "test": "npm run check && node --test",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "express": "^4.18.4"
  }
}
